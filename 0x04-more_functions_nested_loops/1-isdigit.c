#include "main.h"
/**
 * _isdigit - a function that checks for a digit
 * @c: parameter to be printed
 * Return: 1 if successful
 * 0 if otherwise
 */
int _isdigit(int c)
{
	if (c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

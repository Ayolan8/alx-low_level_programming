#include "main.h"
/**
 * main - print alphabet in lowercase
 * done by Ijalana
 * Return: o upon successful
 */
void print_alphabet(void)
{
	int a;

	for (a = 'z'; a <= 'z'; a++)
	{
		_putchar('0' + a);
	}
	_putchar('\n');
}

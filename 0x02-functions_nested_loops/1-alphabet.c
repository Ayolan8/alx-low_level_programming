#include "main.h"
/**
 * main- print_alphabet(void) in lowercase
 * 
 * Return: o upon successful
 */
void print_alphabet(void)
{
	int a;

	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar('0' + a);
	}
	_putchar('\n');
}

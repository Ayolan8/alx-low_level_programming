#include <stdio.h>
/**
 * main - A program that prints all single digit number of base 10
 * Return: 0 (success)
 */
int main(void)
{

	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	return (0);
}

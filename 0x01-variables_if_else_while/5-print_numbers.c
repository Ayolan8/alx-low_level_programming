#include <stdio.h>
/**
 * main - A program that prints all single digit number of base 10
 * Return: 0 (success)
 */
int main(void)
{

	int x;

	x = 0;
	while
		(x < 10)
		{
			printf("%d", x);
			x++;
		}
	putchar('\n');
	return (0);
}

#include <stdio.h>
/**
 * main - A C program that prints the lowercase alphabet in reverse
 * Return: 0 (Success)
 */
int main(void)
{
	char x;

	x = 'z';
	while (x >= 'a')
	{
		putchar(x);
		c--;
	}
	putchar('\n');
	return (0);
}
